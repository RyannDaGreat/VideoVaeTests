# Zoom-Out IC LoRA Training Config (auto-generated by train.sh)
model:
  model_path: "/root/CleanCode/Github/VideoVaeTests/LTX2/models/ltx-2-19b-dev.safetensors"
  text_encoder_path: "/root/CleanCode/Github/VideoVaeTests/LTX2/models/gemma-3-12b-it-qat-q4_0-unquantized"
  training_mode: "lora"

lora:
  rank: 32
  alpha: 32
  dropout: 0.0
  target_modules:
    - "attn1.to_k"
    - "attn1.to_q"
    - "attn1.to_v"
    - "attn1.to_out.0"
    - "attn2.to_k"
    - "attn2.to_q"
    - "attn2.to_v"
    - "attn2.to_out.0"
    - "ff.net.0.proj"
    - "ff.net.2"

training_strategy:
  name: "video_to_video"
  first_frame_conditioning_p: 0.2
  reference_latents_dir: "reference_latents"

optimization:
  learning_rate: 2e-5
  steps: 100000
  batch_size: 1
  gradient_accumulation_steps: 1
  max_grad_norm: 1.0
  optimizer_type: "adamw"
  scheduler_type: "linear"
  scheduler_params: {}
  enable_gradient_checkpointing: true

acceleration:
  mixed_precision_mode: "bf16"
  quantization: null
  load_text_encoder_in_8bit: false

data:
  preprocessed_data_root: "/root/CleanCode/Github/VideoVaeTests/Training/ZoomOutEnvatoTest/datasets/.precomputed"
  num_dataloader_workers: 2

validation:
  prompts:
    - " The video features a detailed examination of an escalator's steps, focusing on the intricate design of the metal steps with parallel lines. The monochromatic color scheme of grays and blacks creates "
    - " Two foxes are nestled within a narrow, earth-toned tunnel, their fur a blend of burnt orange and white. Initially, one fox lies on the ground, its head turned towards the viewer, while the other stan"
    - "A woman with blonde hair, dressed in a light blue shirt and casual jeans, is seated on an orange couch, engaging in a video call with a doctor. The doctor, visible on the tablet screen, gestures with "
    - "A woman in a beige top and olive green trousers is in a modern, minimalist interior, engaging in a serious conversation with a young boy in a white t-shirt and blue plaid shirt. They stand close, some"
  reference_videos:
    - "/root/CleanCode/Github/VideoVaeTests/Training/ZoomOutEnvatoTest/datasets/test_reference_videos/ZJBR6K7.mp4"
    - "/root/CleanCode/Github/VideoVaeTests/Training/ZoomOutEnvatoTest/datasets/test_reference_videos/J2MZ4VE.mp4"
    - "/root/CleanCode/Github/VideoVaeTests/Training/ZoomOutEnvatoTest/datasets/test_reference_videos/MMJM6TL.mp4"
    - "/root/CleanCode/Github/VideoVaeTests/Training/ZoomOutEnvatoTest/datasets/test_reference_videos/RMQGSRF.mp4"
  video_dims: [512, 320, 121]
  frame_rate: 24.0
  inference_steps: 30
  guidance_scale: 4.0
  stg_scale: 1.0
  stg_blocks: [29]
  stg_mode: "stg_v"
  interval: 500
  include_reference_in_output: true
  videos_per_prompt: 1
  seed: 42
  generate_audio: false
  skip_initial_validation: false

checkpoints:
  interval: 500
  keep_last_n: -1
  precision: "bfloat16"

flow_matching:
  timestep_sampling_mode: "shifted_logit_normal"
  timestep_sampling_params: {}

output_dir: "/root/CleanCode/Github/VideoVaeTests/Training/ZoomOutEnvatoTest/outputs"
seed: 42
